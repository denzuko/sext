; qlfile -- qlot dependency manifest for sext
; Run: qlot install
;
; NOTE for next session: confirm whether Concrete-Syntax-Tree and
; Cleavir's CST-to-AST system are present in the default Quicklisp
; dist, or whether they need to be pulled from Ultralisp / git source
; directly (Cleavir has historically been distributed more often via
; direct git clone + ASDF registry push than via Quicklisp proper --
; verify before assuming `ql cleavir` resolves).

ql fiveam
ql com.inuoe.jzon
ql trivial-json-codec
ql shasht

; Likely needs git-source entries, e.g.:
; git concrete-syntax-tree https://github.com/s-expressionists/Concrete-Syntax-Tree.git
; git cleavir https://github.com/s-expressionists/Cleavir.git
; CONFIRM AND UNCOMMENT / CORRECT NEXT SESSION.

ultralisp 40ants-doc
